# Copy to .env and fill in. Loaded automatically by main.py (python-dotenv).

# Required for OpenAI cloud models (gpt-*):
OPENAI_API_KEY="sk-..."

# Local vLLM servers (dgx01/dgx02) ignore the API key. Override the dummy only
# if your server enforces one:
# LOCAL_API_KEY="dummy"
